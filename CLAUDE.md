# CLAUDE.md

@AGENTS.md

This repository keeps its agent guidance in AGENTS.md, which is the filename coding agents other
than Claude Code look for. The bare `@` line above is a Claude Code import directive, not a
decorative link — it pulls that file in directly, so there is nothing further to read here. Leave
it unquoted and outside any code span; an import inside backticks or a fenced block is ignored.
